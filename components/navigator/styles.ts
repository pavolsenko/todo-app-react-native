import {CSSProperties} from "react";
import {Theme} from "@react-native-material/core";

export const headerStyles = (theme: Theme): CSSProperties => ({
    backgroundColor: theme.palette.primary.main,
});